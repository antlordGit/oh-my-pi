-- ===========================================================================
-- V8: 用户增加姓名字段（展示用，username 仅用于登录）
-- ===========================================================================

ALTER TABLE users ADD COLUMN name VARCHAR(64) NULL COMMENT '姓名（展示用）' AFTER username;
