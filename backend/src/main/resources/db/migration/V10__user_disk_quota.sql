-- ===========================================================================
-- V10: 用户磁盘空间限额（单位 MB，默认 100）
-- ===========================================================================

ALTER TABLE users ADD COLUMN disk_limit_mb INT NOT NULL DEFAULT 100 COMMENT '磁盘空间限额（MB）';
