-- ===========================================================================
-- V7: 模型配置增加显示名称字段
-- ===========================================================================

ALTER TABLE model_config ADD COLUMN display_name VARCHAR(128) NULL COMMENT '显示名称' AFTER config_name;
