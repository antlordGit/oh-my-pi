-- ===========================================================================
-- V6: 模型配置增加备注字段
-- ===========================================================================

ALTER TABLE model_config ADD COLUMN remark VARCHAR(512) NULL COMMENT '备注' AFTER config_json;
